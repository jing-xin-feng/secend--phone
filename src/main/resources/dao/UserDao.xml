<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.UserDao">
    <select id="findByUsername" resultType="model.User">
        SELECT id, username, password, email, phone FROM user WHERE username = #{username}
    </select>
    <select id="findByEmail" resultType="model.User">
        SELECT id, username, password, email, phone FROM user WHERE email = #{email}
    </select>
    <select id="findByPhone" resultType="model.User">
        SELECT id, username, password, email, phone FROM user WHERE phone = #{phone}
    </select>
    <insert id="save" parameterType="model.User">
        INSERT INTO user(username, password, email, phone) 
        VALUES(#{username}, #{password}, #{email}, #{phone})
    </insert>
</mapper>